# media/

- `icon.svg` — Activity Bar icon (used inside VSCode at runtime).
- `icon.png` — Marketplace listing icon. **Currently a 1×1 placeholder.**
  Replace with a 128×128 (or 256×256) PNG with transparent background.
  See README → Building / icon design prompt for the brief.
